{
  "tasks": {
    "start": {
      "name": "Start Static Server",
      "command": "npm install && npm run build && npx -y serve@latest out",
      "runAtStart": true,
      "preview": {
        "port": 3000
      }
    }
  }
}
